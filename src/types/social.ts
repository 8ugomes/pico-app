export type SportId = "futevolei" | "beach-tennis" | "volei-praia";
export type Level = "Iniciante" | "Intermediário" | "Avançado";
export type Sport = { id: SportId; name: string; shortName: string };
export type Player = { id: string; username: string; name: string; bio: string; neighborhood: string; avatar: number; sports: { sportId: SportId; level: Level }[]; arenaIds: string[]; available: boolean };
export type Arena = { id: string; slug: string; name: string; neighborhood: string; city: string; description: string; sports: SportId[]; members: number; image: string; imagePosition: string; amenities: string[] };
export type Post = { id: string; authorId: string; arenaId: string; sportId: SportId; content: string; photo?: string; createdAt: number; likes: number; gamePlayedOn?: string; communityIds?: string[]; audience?: "beta" | "private"; distributedToArena?: boolean };
export type Comment = { id: string; postId: string; authorId: string; content: string; createdAt: number };
export type DemoGame = { id: string; playerId: string; arenaId: string; sportId: SportId; playedOn: string; createdAt: string; version: number };
export type DemoCommunity = { id: string; slug: string; name: string; description: string; rules: string; sports: SportId[]; arenaId?: string; visibility: "beta" | "private"; entryMode: "open" | "approval" | "invite"; members: string[]; pending: string[] };
export type DemoSeed = { currentUserId: string; sports: Sport[]; players: Player[]; arenas: Arena[]; posts: Post[]; comments: Comment[]; games: DemoGame[]; communities: DemoCommunity[] };
export type DemoState = DemoSeed & { deletedGameIds: string[]; likedPostIds: string[]; connectedPlayerIds: string[]; followedArenaIds: string[] };
